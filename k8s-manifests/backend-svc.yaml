apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: app
spec:
  selector:
    app: backend
  type: NodePort
  ports:
  - name: service-port
    port: 80
    targetPort: 5050
    nodePort: 30100